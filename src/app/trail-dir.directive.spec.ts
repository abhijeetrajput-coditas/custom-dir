import { TrailDirDirective } from './trail-dir.directive';

describe('TrailDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TrailDirDirective();
    expect(directive).toBeTruthy();
  });
});
